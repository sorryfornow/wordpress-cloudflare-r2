{
  "name": "wordpress-r2",
  "version": "1.0.0",
  "description": "WordPress on Cloudflare Container with R2 persistence",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "r2:create": "wrangler r2 bucket create wordpress-data",
    "r2:list": "wrangler r2 bucket list",
    "containers:list": "wrangler containers list",
    "logs": "wrangler tail"
  },
  "dependencies": {
    "@cloudflare/containers": "^0.0.5"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241205.0",
    "typescript": "^5.5.0",
    "wrangler": "^4.0.0"
  }
}
